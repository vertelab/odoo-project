<odoo>
    <data>
        <record id="mail_channel_form_view" model="ir.ui.view">
            <field name="name">mail.channel.form</field>
            <field name="model">mail.channel</field>
            <field name="inherit_id" ref="mail.mail_channel_view_form"/>
            <field name="arch" type="xml">
                <field name="description" position="before">
                    <field name="calendar_event_id"/>
                </field>
            </field>
        </record>

        <record id="project_mail_channel_view_kanban" model="ir.ui.view">
            <field name="name">mail.channel.kanban</field>
            <field name="model">mail.channel</field>
            <field name="priority" eval="10"/>
            <field name="arch" type="xml">
                <kanban>
                    <field name="id"/>
                    <field name="description"/>
                    <field name="is_member"/>
                    <field name="group_ids"/>
                    <templates>
                        <t t-name="kanban-description">
                            <div class="oe_group_description" t-if="record.description.raw_value">
                                <field name="description"/>
                            </div>
                        </t>
                        <t t-name="kanban-box">
                            <div class="oe_module_vignette oe_kanban_global_click d-flex align-items-center">
                                <img t-att-src="kanban_image('mail.channel', 'avatar_128', record.id.raw_value)" class="oe_module_icon" alt="Channel"/>
                                <div class="oe_module_desc">
                                    <h4 class="o_kanban_record_title">#<field name="name"/></h4>
                                    <p class="oe_module_name">
                                        <field name="description"/>
                                    </p>
                                    <button type="object" class="btn btn-secondary float-end" name="action_view_chat">Open Channel</button>
                                    <button type="object" class="btn btn-secondary float-start" name="action_download_chat">Chat Report</button>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>
    </data>
</odoo>